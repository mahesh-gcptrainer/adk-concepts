name: 'infra-designer'
acli_version: '1.6.1'
agent_directory: 'app'
region: 'us-central1'
base_template: 'adk'
generated_at: '2026-09-17T06:44:08.986225+00:00'
language: 'python'
create_params:
  # The ADK name of the root agent, as rendered into the agent source. Rename
  # the agent and you must change it in both places: telemetry reports this as
  # gen_ai.agent.name, and it is what agents-cli reports for the project.
  root_agent_name: 'infra_designer'
  deployment_target: 'cloud_run'
  session_type: 'in_memory'
  cicd_runner: 'google_cloud_build'
  is_a2a: true
  agent_gateway: False
  agent_guidance_filename: 'GEMINI.md'
